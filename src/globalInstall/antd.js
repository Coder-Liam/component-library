import { Button, Input, Select, List, ListItem, Table, Checkbox } from "ant-design-vue"

export default (app) => {
  app.use(Button)
  app.use(Input)
  app.use(Select)
  app.use(List)
  app.use(ListItem)
  app.use(Table)
  app.use(Checkbox)
}
